import * as mocha from "mocha";
import * as assert from "assert"
import { StaticFile } from "../src/model/staticFile"

describe('constructor', () => {
  const file = StaticFile.generate('path/to/file', 'path/to/source')
})
